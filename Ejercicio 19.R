x1 = as.integer(readline(prompt="Digite la cordenada: "))
x2 = as.integer(readline(prompt="Digite la cordenada: "))
y1 = as.integer(readline(prompt="Digite la cordenada: "))
y2 = as.integer(readline(prompt="Digite la cordenada: "))

d=sqrt(((x2-x1)**2)+((y2-y1)**2))
paste("La distancia de las cordenadas es",d)